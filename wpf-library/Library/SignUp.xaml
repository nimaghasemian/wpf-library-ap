<Window x:Class="Library.SignUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="SignUp" Height="600" Width="483" ResizeMode="NoResize">
    <Window.Resources>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" SnapsToDevicePixels="true" StrokeThickness="1" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="Sty-roundbtn" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="16" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="0,0,-20,0">
        <Label Content="SIGN UP" HorizontalAlignment="Center" Margin="0,31,0,0" VerticalAlignment="Top" FontSize="45" Height="65" FontFamily="Showcard Gothic"/>
        <TextBox Style="{DynamicResource Sty-signup-fields}" Name="nameInput" HorizontalAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Top" Width="203" Height="22" Margin="0,126,0,0" TextChanged="TextBox_TextChanged" FontSize="14"/>
        <TextBox Name="emailInput" HorizontalAlignment="Center" Margin="0,163,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="203" Height="22" Style="{DynamicResource Sty-signup-fields}" FontSize="14"/>
        <PasswordBox Name="passwordInput" HorizontalAlignment="Center" Margin="0,199,0,0" VerticalAlignment="Top" Width="203" Height="22" FontSize="14"/>
        <PasswordBox Name="confirmPasswordInput" HorizontalAlignment="Center" VerticalAlignment="Top" Width="203" Height="22" Margin="0,234,0,0" FontSize="14"/>
        <Label Content="Name:" HorizontalAlignment="Left" Margin="33,126,0,0" VerticalAlignment="Top" Width="44"/>
        <Label Content="Email:" HorizontalAlignment="Left" Margin="33,161,0,0" VerticalAlignment="Top"/>
        <Label Content="Password:" HorizontalAlignment="Left" Margin="33,197,0,0" VerticalAlignment="Top"/>
        <Label Content="Confirm Password:" HorizontalAlignment="Left" Margin="33,232,0,0" VerticalAlignment="Top" Width="107"/>
        <TextBox Name="phoneInput" HorizontalAlignment="Center" Margin="0,269,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="203" Height="22" Style="{DynamicResource Sty-signup-fields}" FontSize="14"/>
        <Label Content="Phone Number:" HorizontalAlignment="Left" Margin="33,267,0,0" VerticalAlignment="Top"/>
        <Button Style="{DynamicResource Sty-roundbtn}" Name="ctnButton" Content="Continue to Payment" HorizontalAlignment="Center" Margin="0,306,0,0" VerticalAlignment="Top" Width="179" Height="37" Click="Button_Click_1" FontWeight="Bold"/>
        <Button Content="Back to Login" BorderThickness="1.5" HorizontalAlignment="Center" Margin="0,351,0,0" VerticalAlignment="Top" Width="95" Click="Button_Click" Height="27" Background="{x:Null}"/>
        <Label Name="errorLabel" Foreground="Red" Content="" HorizontalAlignment="Center" Margin="0,397,0,0" VerticalAlignment="Top" Width="382" Height="125"/>

    </Grid>
</Window>
